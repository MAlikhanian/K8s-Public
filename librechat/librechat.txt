helm install <deployment-name> oci://ghcr.io/danny-avila/librechat-chart/librechat

helm install librechat oci://ghcr.io/danny-avila/librechat-chart/librechat --namespace librechat

helm install librechat oci://ghcr.io/danny-avila/librechat-chart/librechat \
  --namespace librechat \
  --values values.yaml

helm upgrade librechat oci://ghcr.io/danny-avila/librechat-chart/librechat \
  -n librechat -f values.yaml

kubectl get pods -n librechat 
kubectl get secrets -n librechat 
kubectl get pvc -n librechat 
kubectl get configmaps -n librechat 
kubectl get ingress -n librechat 
kubectl get jobs -n librechat 
kubectl get cronjobs -n librechat 
kubectl get svc -n librechat 
kubectl get certificate -n librechat 
kubectl get sts -n librechat 

kubectl delete pod -n librechat --all
kubectl delete pvc -n librechat --all

# get the last events first (often the fastest clue)
kubectl get events -n librechat --sort-by=.lastTimestamp | tail -60

# app pod logs (why it’s crashing)
kubectl logs -n librechat deploy/librechat-librechat --previous

# meilisearch config error details
kubectl describe pod -n librechat librechat-meilisearch-0

# mongodb init container image + error
kubectl describe pod -n librechat librechat-mongodb-9b6bb6f48-f8wnk

--------------------------------------------------------------------------------------------
# 1. Create the required secret with all necessary keys
kubectl create secret generic librechat-credentials-env \
  --from-literal=CREDS_KEY=$(openssl rand -hex 16) \
  --from-literal=CREDS_IV=$(openssl rand -hex 8) \
  --from-literal=JWT_SECRET=$(openssl rand -hex 16) \
  --from-literal=JWT_REFRESH_SECRET=$(openssl rand -hex 16) \
  --from-literal=MEILI_MASTER_KEY=$(openssl rand -hex 16) \
  --from-literal=OPENAI_API_KEY="sk-your-actual-openai-key-here" \
  --namespace librechat

# 2. Verify the secret was created
kubectl get secret -n librechat librechat-credentials-env
kubectl describe secret -n librechat librechat-credentials-env
--------------------------------------------------------------------------------------------
# Create Docker registry secret for Nexus
kubectl create secret docker-registry docker-secret \
  --docker-server=docker.sebaoffice.ir \
  --docker-username=0082280665 \
  --docker-password=ABcd@12345 \
  --namespace librechat

# Then add to values.yaml at the top level:
imagePullSecrets:
  - name: nexus-docker-secret
--------------------------------------------------------------------------------------------





